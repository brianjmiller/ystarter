    /**
        Provides a base App View extended with View Base

        @module ystarter-app
        @submodule ystarter-app-v-base
    */

    /**
        Base App View implementation

        @class YStarter.App.V.Base
        @constructor
        @extends YStarter.V.Base
    */
    var Clazz = Y.namespace("YStarter.App.V").Base = Y.Base.create(
        NAME,
        Y.YStarter.V.Base,
        [],
        {},
        {
            ATTRS: {}
        }
    );
